language: swift
osx_image: xcode9.4

script:
  - set -o pipefail && travis_retry xcodebuild -project Bond.xcodeproj -scheme Bond-iOS -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 7,OS=11.3' test | xcpretty
  - swift test

after_success:
  - bash <(curl -s https://codecov.io/bash)
